// Regions tab (skeleton)
(function(){
  if (!window.Preproc) window.Preproc = {};
  function init(ctx){
    // Leave complex ROI logic to future extraction; avoid blocking errors
    return {};
  }
  window.Preproc.Regions = { init };
})();

